let numbers = [1, 2, 3, 4, 5, 6];
let languages = ["C", "C++", "Java", "Python", "JavaScript"];

numbers.reverse();      // changes the original array
languages.reverse();    // changes the original array

console.log(numbers);
// [1, 2, 3, 4, 5, 6]

console.log(languages);
// ['JavaScript', 'Python', 'Java', 'C++', 'C']