const age = [8, 3, 49, 25, 4, 8, 12];

// returns elements array that satisfies the condition
const found = age.filter((element) => element > 10);

console.log(found);
// Expected output: [49, 25, 12]